from django import forms


# class AddCommentForm(forms.Form):

# #폼에 필드가 하나이기때문에 그냥 템플릿에서 input을 쓰기로함
#     message = forms.CharField(
#         required=True, widget=forms.TextInput(attrs={"placeholder": "Add a Comment"})
#     )
